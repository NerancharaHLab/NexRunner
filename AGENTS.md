# กฎบังคับในการทำงาน (Mandatory Execution Rules)

## กฎบังคับ
- **ห้ามลงมือ implement งานใดๆ ทันทีโดยไม่ผ่าน `EnterPlanMode` + แตก Phase/Task ลง `TODO.md` ก่อนเริ่มเขียนโค้ด — ไม่มีข้อยกเว้นสำหรับ "งานเล็ก"** (อัปเดต: ผู้ใช้ยืนยันชัดเจนแล้วว่าต้องการแบบเข้มนี้ แม้เป็นงานแค่ไฟล์เดียว เช่น แก้ Text/CSS จุดเดียว ก็ต้องผ่านขั้นตอนนี้ก่อนเสมอ — Apply กับทุก Session/ทุก Agent ที่ทำงานในโปรเจกต์นี้ ไม่ใช่แค่ Session ที่ได้รับคำสั่งนี้ครั้งแรก):
  - ก่อนเริ่มเขียนโค้ดทุกครั้ง (ไม่ว่างานจะเล็กหรือใหญ่แค่ไหน) ต้องเข้า `EnterPlanMode` ก่อน ร่าง Plan (แม้เป็น Plan สั้นๆ 1-2 บรรทัดสำหรับงานเล็ก ก็ต้องมี) แล้วขอ Sign-off ผ่าน `ExitPlanMode`
  - แตกงานเป็น Task/Phase ที่ทำได้จริงทีละอัน บันทึกลง `TODO.md` ก่อนเริ่มทำ (แม้งานเล็กจะมี Task เดียวก็ต้องบันทึกก่อนเริ่ม ไม่ใช่บันทึกย้อนหลังหลังทำเสร็จ)
  - ถ้ามีทางเลือก/แนวทางที่ถูกต้องได้มากกว่า 1 แบบ (เช่น เลือก Library, เลือกรูปแบบ Auth, เลือก State Shape, หรือแม้แต่ถ้อยคำ/Copy ที่จะใช้) หรือ Requirement ยังไม่ชัดพอ ให้ใช้ `AskUserQuestion` ถามก่อนเข้า Plan Mode หรือระหว่าง Plan Mode ก็ได้
  - เป็น Action ที่ย้อนกลับยากหรือกระทบข้อมูลจริงภายนอก (เขียนข้อมูลจริงลง Linear, ลบ/เขียนทับของเดิม, Deploy จริง) ยิ่งต้องผ่านขั้นตอนนี้เข้มงวดกว่าปกติ
  - ทำแนวทางไปแล้วครึ่งทางเจอว่าของจริงไม่ตรงกับที่ Premise ไว้ตอนแรก (เช่น Library มี API ต่างจากที่คิด, Service มี State ไม่ตรงที่คาด) — ต้องหยุดแล้วกลับเข้า Plan Mode ใหม่ ไม่ใช่เดาต่อเอง

- **สร้าง/แก้ไข UI ทุกครั้งต้องอ่าน skill `datatest-id-standard` ก่อนเสมอ** (`.agents/skills/datatest-id-standard/SKILL.md`) — ทุก element ที่ต้องกดหรือ assert ได้ (ปุ่ม, input, select, table row, modal ฯลฯ) ต้องใส่ `data-testid` ตาม pattern `[module]:[component]:[element]__[modifier]` ที่ระบุไว้ในนั้น ห้ามตั้งชื่อ `data-testid` เอาเองโดยไม่ผ่าน skill นี้ ไม่ว่าจะเขียน UI ใหม่ในโปรเจกต์นี้ (เช่น `web/`) หรือแก้ของเดิม

- **การสร้างสคริปต์ชั่วคราว (Temp/Helper Scripts) ห้ามสร้างไว้ที่ Root ของโปรเจกต์** — สคริปต์ช่วยเหลือ, สคริปต์เจนเนอเรต, สคริปต์ย้ายข้อมูล หรือสคริปต์ทดสอบชั่วคราว (เช่น `.py`, `.sh`, `.js` ชั่วคราว) ต้องถูกสร้างและจัดเก็บไว้ในโฟลเดอร์ `temp_scripts/` เสมอ เพื่อรักษาความสะอาดของโครงสร้าง Root ของโปรเจกต์

- **เนื้อหาที่ผู้ใช้ระบุว่าเป็น "แพลน" (Plan) หรือ "Roadmap" คือบันทึกทิศทาง ไม่ใช่คำสั่งให้ลงมือทำ** — เช่นหัวข้อ "แผนการพัฒนาในอนาคต (Roadmap)" ใน `web/README.md` (Docker/Data Persistence, Site Management UI, Dynamic Config, Production Cloud Deployment ฯลฯ) **ห้ามเพิ่ม/แก้โค้ดใดๆ ตาม Item ในนั้นทันที** แม้ Item จะดูเหมือน "เล็กน้อย" ตามเกณฑ์ข้อแรกก็ตาม ต้องรอให้ผู้ใช้สั่งเจาะจงก่อนเสมอว่าจะให้เริ่ม Item ไหน — กฎนี้ Apply กับทุก Session/ทุก Agent ที่ทำงานในโปรเจกต์นี้ ไม่ใช่แค่ Session ที่ได้รับคำสั่งนี้ครั้งแรก

- **Git Flow & Branching Model (Trunk-based Development)**:
  - **Trunk-based**: กิ่งหลักคือ `main` เท่านั้น โดย `main` ต้องอยู่ในสถานะที่ Build ผ่านและ Test ผ่านเสมอ
  - **Short-lived Branches**: แตก Branch ย่อยสั้น ๆ จาก `main` เช่น `feat/*`, `fix/*`, `chore/*`, `test/*` แล้วรวมกลับเข้า `main` ทันทีหลังทำเสร็จและ Test ผ่าน
  - **Conventional Commits**: ใช้ฟอร์แมต `type(scope): subject` เสมอ (เช่น `feat(web): ...`, `fix(auth): ...`, `chore(git): ...`)
  - **Production Release**: การ Release ขึ้น Production ให้สร้าง Git Tag บน `main` เช่น `git tag -a v1.0.0 -m "Release v1.0.0"`
